import SectionTitle from "@/components/SectionTitle/SectionTitle";

const CreateProduct = () => {
    return (
        <div>
            <SectionTitle header="Add New Product"/>
        </div>
    );
};

export default CreateProduct;